{
  "scripts": {
    "tsc:init": "tsc --init",
    "build": "tsc",
    "test": "tsc && jest dist/"
  },
  "devDependencies": {
    "@commitlint/cli": "^12.1.4",
    "@commitlint/config-conventional": "^12.1.4",
    "@types/jest": "^26.0.24",
    "husky": "^6.0.0",
    "jest": "^27.2.3"
  },
  "dependencies": {
    "typescript": "^4.4.3"
  }
}
